def lowercase_first_letter(string):
    if string:
        return string[0].lower() + string[1:]
    else:
        return string

# 测试代码
input_string = input("请输入一个字符串：")
result = lowercase_first_letter(input_string)
print("首字母小写后的字符串：", result)
